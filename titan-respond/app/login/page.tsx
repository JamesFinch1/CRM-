'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);const router=useRouter(); async function submit(e:any){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json().catch(()=>({}));setBusy(false);if(!r.ok)return setError(d.error||'Login failed');router.push('/');router.refresh()} return <main className="loginWrap"><section className="loginCard"><span className="eyebrow">TITAN DIGITAL</span><h1>Titan Respond</h1><p>Secure client workspace</p>{error&&<div className="error">{error}</div>}<form onSubmit={submit}><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required minLength={8}/><button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form></section></main>}
