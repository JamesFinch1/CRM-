services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: titan
      POSTGRES_USER: titan
      POSTGRES_PASSWORD: titan
    ports: ["5432:5432"]
    volumes: ["titan_postgres:/var/lib/postgresql/data"]
volumes:
  titan_postgres:
